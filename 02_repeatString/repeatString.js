const repeatString = function(str, num) {
    let str0 = str
    for (let i = 1; i < num; ++i) {gi
        str0 = str0 + str;
    }
    return str0;
};

// Do not edit below this line
module.exports = repeatString;
